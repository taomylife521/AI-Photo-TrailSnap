## 重建缩略图任务优化

1. package\server\app\service\tasks\thumbnail.py 重建缩略图任务两个类型合并成一个，根据payload判断是否需要扫描，参考package\server\app\service\tasks\basic.py
2. 支持批量处理，实现process_batch方法

---

## 官网新增trailsnap-cli的页面

1. package\official-site新增关于trailsnap-cli的页面，路由为/cli，需要支持多语言
2. 页面的设计可以参考https://lobehub.com/zh/cli
3. 内容参考package\trailsnap-cli

---

人物删除功能

package\website\src\views\album\people\PeopleList.vue 新增删除按钮，点击后弹出确认删除窗窗，确认后删除人物
package\server\app\api\face.py 删除人物接口不再使用软删除，直接删除数据库里的数据

---

智能分类封面设置功能

1. package\server\app\db\models\tag.py photo_tags表新增一个封面字段，类型为uuid，用于存储人物的封面照片id，删除照片时，将封面字段设为null
2. package\server\app\api\classification.py 新增设置分类封面接口，用于设置分类的封面照片
3. package\website\src\views\album\intelligent-classification\ClassificationDetail.vue 新增设置封面按钮，与package\website\src\views\album\people\PeopleDetail.vue类似
4. package\server\app\api\classification.py 查询分类列表的时候如果某个分类的封面为空，则把第一个照片作为封面
5. 仅修改数据库字段即可，不要使用alembic迁移，后面我会自己迁移数据库

之前的逻辑是photo_tags表里的owner_id字段为空则认为是所有用户的公共分类，加上cover_id字段后，封面会产生权限错误，现在要移出公共的tag，为每个用户都新建一个tag

修改package\server\alembic\versions\b21a07c0b227_0023_add_cover_id_to_tag.py数据库迁移脚本
1. 删除所有没有关联照片且owner_id为空的tag
2. 查询所有owner_id为空的tag，查找photo_tag_relations表中对应的记录，按photo的owner_id分组，为每个owner_id新建一个tag，owner_id为owner_id，tag_name为tag_name，cover_id为第一个photo，然后更新photo_tag_relations表的tag_id字段为新建的tag的id
3. 删除所有owner_id为空的tag
4. 提交事务