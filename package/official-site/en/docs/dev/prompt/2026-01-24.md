# 2026-01-24

Location album optimization:

1. `package\website\src\views\album\location\LocationList.vue` map view: add “Total unlocked: xxx provinces, xxx cities, xx% unlocked” at top-left
2. On mobile, replace “district/city” switch buttons with a dropdown to save space

---

Bug fix:

1. `package\server\app\service\tasks\scan.py` scanning a single folder currently scans all images instead of only that folder, which may delete photos outside the target folder

---

Task performance analysis:

1. `package\server\app\service\tasks\basic.py` basic task is slow — analyze the reason and propose solutions

---

Task management optimization:

1. `package\website\src\views\settings\TaskManagement.vue` add a “Delete Failed Tasks” action with confirmation
2. Add a backend API to delete failed tasks

---

Annual report optimization:

1. `package\server\app\api\annual_report.py` `get_report_season` should pick the highest-quality photo instead of the first photo
2. `get_report_easter_egg` should pick the highest-quality photo instead of random
3. Use vector search on `image_vectors` table: filter by time, exclude screenshots and videos, then pick the most similar photo
4. Provide example vectors in code (multiple vectors may be used for different scenarios, e.g. seasons, best photos for different contexts)

---

Annual report optimization:

1. In `package\server\app\crud\annual_report.py`, `get_report_emotion` should make `CarouselGroup` include multiple scenes. For each scene, pick the top 3 photos by similarity, threshold 0.28. Skip scene if none.

---

Implement update checking:

1. Add a JSON file in `package\official-site` to store version and update notes, e.g.

```json
{
  "version": "1.0.0",
  "update_info": "Fixed some bugs and added new features"
}
```

2. Fetch the JSON and compare with current version; if newer, show update dialog and aggregate update info between versions
3. Dialog should show changelog and link to download page
4. Backend returns latest version/update info; frontend decides whether to show dialog

---

Ticket task optimization:

1. Add `photo_id` to `train_tickets` (nullable). One photo can have multiple tickets
2. In `package\server\app\service\tasks\tickets.py`, when processing a photo, delete tickets of that photo first, then reprocess

---

Import scenic spot data:

1. In `package\server\start.py`, after migrations, import `package\server\resources\scenes_5A.csv`, dedupe, and remove `created_at` column
2. Add a field to `scenes` to distinguish user-defined vs built-in; allow deleting only user-defined scenes
3. In `package\website\src\views\album\location\LocationList.vue`, support editing/deleting user-defined scenes (name, description, etc.)

---

Improve image viewer interactions:

1. When the side panel (details/OCR etc.) is open, page navigation buttons are covered. Adjust layout so the side panel is not absolutely positioned when expanded
